# Build outputs
*.img
*.bin
# ...but the committed font asset is a source, not a build output
!stage2/font8x8.bin
# ...and so is the committed, frozen test component of Stage 5
!stage5/component.bin
*.o
*.elf
*.lst
*.map

# Test artefacts
stage0/out/
out/
*.ppm
*.png
*.log
serial.txt

# Python bytecode from importing the checkers
__pycache__/
*.pyc

# The history screenshots are content, not build output
!history/*.png

# The germline - the machine's own cache of proven components, never repo content
germline/
